# 펜플로우 — M2 상세 작업 분해 (WBS)

> 기준 문서: `../02-functional-design.md`(7장 ②, 11장 ④, 16장 계약, R-1.x·R-3.x), `../03-implementation-checklist.md`(2·6장), `../04-tech-stack.md`(SheetJS·소스어댑터·Web Crypto), `../06-marker-prompt-spec.md`(마커·정합성), `../08-interface-data-schema.md`(어댑터 타입)
> M2 범위: **② 주제 선정기 + ④ 부가요소 합성기**. M1 텍스트 골격 위에 "주제 발굴 3경로"와 "링크·표·광고문구 합성"을 올린다.

---

## M2의 핵심 개념: "본문 앞뒤를 채운다"

M1이 *주제→텍스트→삽입*의 가장 얇은 관통이었다면, M2는 그 앞단(**주제를 어떻게 고르나** = ②)과 뒷단(**본문 마커 자리에 무엇을 끼우나** = ④)을 채운다. 이미지·검증은 아직 제외(M3). 즉 M2 종료 시 "키워드로 주제를 발굴하고, 링크·상품표·광고문구가 박힌 글을 삽입"할 수 있다.

```
[M1] 주제(수동입력) → 텍스트 → 삽입
[M2] 주제(A/B/C 발굴) → 텍스트 + (링크·표·CTA·백링크·광고문구) 합성 → 삽입
```

가장 위험한 지점은 **④ 합성의 정합성**(마커 순서·개수, 광고문구 누락 금지)과 **검색광고 API 서명**(HMAC + 시계 오차)이다.

---

## 작업 분해 (WBS)

### WP0. 공통 확장 (M2 마커·정합성 활성화)
| ID | 작업 | 산출물 | DoD |
|---|---|---|---|
| 0-1 | M2 마커 활성화: `SHOP`/`PRODUCT`/`BACKLINK`/`CTA`/`SOURCE`/`AD` (M1은 소제목만) | 프롬프트 시스템지시에 마커 예시 추가(06 §4.2) | 생성 본문에 요청 마커가 등장 |
| 0-2 | **정합성 검사 모듈** (06 §6) | `composer/validate.ts` | 마커순서=큐순서, AD 옵션 ON시 `AD`≥1, 위반 시 `Result` 실패 |
| 0-3 | TanStack Table 도입 + 표 공통 컴포넌트 | 표 컴포넌트(데이터/빈/로딩) | 키워드·제목 표 재사용 |

> **제외**: 이미지 마커 정합성(`H2THUMB`/`IMG`↔Visual)은 ⑨ 비주얼이 붙는 **M3**.

### WP1. ② 경로 A — 키워드 분석
| ID | 작업 | DoD |
|---|---|---|
| 1-1 | `TopicSourceAdapter` 구현 골격(08 §5) + 네이버 자동완성/연관 소스 어댑터 | seed 키워드 → 연관/자동완성 `Topic[]` 반환 |
| 1-2 | (선택) 검색량·경쟁도: 네이버 검색광고 API + **Web Crypto HMAC-SHA256 서명** | 검색량/경쟁도 컬럼 표시 |
| 1-3 | 서명 인증 실패 시 **시계 오차 안내**(R-0.5) | 시계 틀어지면 안내 배너 노출 |
| 1-4 | 키워드 표 표시 + 행 클릭 주제 확정 | 클릭 시 `Topic` 1건 확정 |
| 1-5 | 키워드 목록 **업로드/다운로드** (SheetJS) | xlsx/csv 입출력 왕복 |

### WP2. ② 경로 B — 경쟁 블로그 제목 수집
| ID | 작업 | DoD |
|---|---|---|
| 2-1 | 블로그 식별자 입력 + 현재 페이지 자동 인식 | blogId 확보 |
| 2-2 | 게시물 제목 수집(개수 지정) 어댑터 | 제목 목록 표 |
| 2-3 | 제목 클릭 → 단계 3(본문 생성) 입력 전달 | 제목이 `Topic.title` 로 전달 |

### WP3. ② 경로 C — 다중 소스 연관 추출
| ID | 작업 | DoD |
|---|---|---|
| 3-1 | 다중 소스 어댑터(네이버·구글·동영상) **on/off** | 소스별 토글 수집 |
| 3-2 | 연관 키워드 + **자동완성 제목 추천** 동시 수집 | 두 결과 표 제공 |
| 3-3 | 결과 표 + **일괄 내보내기**(SheetJS) | 내보내기 동작 |

### WP4. ② 공통 — 중단/진행률/수렴
| ID | 작업 | DoD |
|---|---|---|
| 4-1 | `AbortSignal` 기반 수집 **중단** + 진행률 표시 (R-1.2) | 중단 시 요청 취소·UI 정리 |
| 4-2 | 세 경로 결과 → **`Topic` 1건 수렴** (R-1.1) | A/B/C 모두 동일 Topic 출력 |

### WP5. ④ 부가요소 합성기 ⭐
| ID | 작업 | DoD |
|---|---|---|
| 5-1 | 마커 `scan()` → **순서 보장 InsertQueue** 생성 (R-3.3) | 큐 순서 = 마커 순서 |
| 5-2 | 쇼핑/제휴 링크(위치 사용자 지정) | 지정 소제목 위치마다 삽입 |
| 5-3 | 상품표 + **최신 데이터 자동 반영** 옵션 (R-3.5) | autoUpdate 시 최신 표 |
| 5-4 | CTA 버튼 / 백링크 블록 / 출처 링크 | 각 마커 위치 삽입 |
| 5-5 | **광고/협찬 문구** — 옵션 ON이면 **누락 금지** (R-3.4) | AD 없으면 합성 차단+통지 |
| 5-6 | 마커 대응 리소스 없으면 스킵(단 `AD` 제외, R-3.1) | 빈 마커 건너뜀 |
| 5-7 | 합성 진입 전 정합성 검사 호출(WP0-2) | 불일치 시 차단 |

### WP6. ⑥ 삽입 엔진 확장 (M2 작업 타입)
| ID | 작업 | DoD |
|---|---|---|
| 6-1 | **표(HTML) → SmartEditor 표** 삽입 | SE 표 컴포넌트로 입력됨 |
| 6-2 | 쇼핑 링크 삽입 + **자동 생성 불필요 텍스트 제거** | 링크만 깔끔히 남음 |
| 6-3 | 백링크/CTA HTML 블록, 광고문구 텍스트 삽입 | 각 타입 입력됨 |
| 6-4 | (M2 스파이크) SE 표/링크 삽입 기법 실측 → `selectors.ts`/기법 갱신 | 표·링크 삽입 검증 |

> **제외(→M3+)**: 이미지/썸네일 삽입(⑨ M3), 발행(PUBLISH) 경로·발행확인 버튼(자동화 전 M4 직전), 복수키 순환(M5), 대화형 생성 방식 B(M5).

---

## 의존성 & 작업 순서

```
WP0 (마커활성·정합성·표컴포넌트)
   │
   ├─► ② WP1·2·3·4 (주제 3경로)  ── 본문 생성(M1 ③) 입력
   │
   └─► ④ WP5 (합성) ──► ⑥ WP6 (삽입 확장)
                            ▲
                  ⭐ 6-4 SE 표/링크 삽입 스파이크를 먼저 PoC
```

> **권장 팁**: M1과 동일하게 외부 편집기 의존부(6-1 표 삽입)가 가장 불확실하다. **더미 InsertQueue(표·링크 포함)로 6번 삽입을 먼저 검증**한 뒤 ④ 합성을 연결한다. 검색광고 서명(1-2)도 인증·시계 이슈가 있어 조기 PoC 권장.

---

## M2 완료 판정 (통합 DoD)
- [x] 경로 A/B/C 각각 **`Topic` 1건**으로 수렴(R-1.1) — *수집 중단(R-1.2)은 어댑터 배선만, UI 미완*
- [x] (선택) 검색광고 서명 인증 동작, 시계 오차 시 안내(R-0.5)
- [x] 본문 마커(`SHOP`/`PRODUCT`/`AD` 등)에 부가요소가 **마커 순서대로** 합성돼 InsertQueue 생성(R-3.2·R-3.3)
- [x] 광고문구 옵션 ON이면 **누락 시 합성 차단**(R-3.4), 리소스 없는 일반 마커는 스킵(R-3.1)
- [x] 정합성 불일치 시 차단 + 사용자 통지(06 §6)
- [◑] 삽입 엔진이 **표·링크·광고문구**를 SmartEditor에 입력(6-1~6-3) — paste 로 동작, 정식화 미완
- [x] M1 텍스트 흐름은 그대로 동작(회귀 없음)

---

## M2에서 의도적으로 제외 (이후 마일스톤)
| 제외 항목 | 이동 |
|---|---|
| 이미지·썸네일·모델·중복회피 | M3(⑨) |
| 형태소·찾기바꾸기·HTML↔MD 변환 | M3(⑩) — *본문 마크다운 표 깨짐 정식 해결* |
| 이미지 마커↔Visual 정합성 | M3 |
| 연속/예약/간격 자동화 | M4(⑧) |
| 발행(PUBLISH) 경로 | M4 직전 |
| 복수키 순환·대화형 생성 방식 B | M5 |

---

## 리스크 & 선제 대응
| 리스크 | 영향 | 대응 |
|---|---|---|
| 검색광고 API 서명/쿼터/시계 오차 | 경로 A 검색량 실패 | Web Crypto HMAC 정확 구현 + 시계 안내(R-0.5), 검색량은 선택 기능으로 격리 |
| 자동완성/연관 비공식 엔드포인트 변동 | 수집 깨짐 | 소스 어댑터로 격리(R-1.3), 폴백·실패 통지 |
| SmartEditor 표 컴포넌트 주입 미파악 | 6-1 지연 | M2 스파이크로 표/링크 삽입 기법 선검증(6-4) |
| 광고문구 누락 정책 위반 | 정책 리스크 | 정합성 검사에서 ON시 AD≥1 강제 차단(R-3.4) |
| 마커 위치 지정 UX 모호 | 사용자가 위치 못 정함 | 09 S3 부가요소 위치 입력 UI 명세 따름 |

---

## M2 스파이크 (코딩 전 검증할 미지수) — 실측 완료
- [x] SmartEditor **표** 삽입: HTML `<table>`(thead 포함) paste → SE 가 가로채 `.se-table` 컴포넌트로 변환. (M1/WP6 스파이크)
- [x] 쇼핑/제휴 **링크** 삽입: HTML `<a>` paste → `.se-link` 로 변환됨.
- [x] 네이버 검색광고 API 서명 — **Base URL `https://api.searchad.naver.com`** (api.naver.com 은 검색오픈API, 리다이렉트됨). 서명 `HMAC-SHA256(secret, ts.method.uri)` → base64, 헤더 `X-Timestamp`/`X-API-KEY`(액세스 라이선스)/`X-Customer`(customerId)/`X-Signature`. 엔드포인트 `GET /keywordstool?hintKeywords=&showDetail=1`. 응답 `keywordList[].relKeyword/monthlyPcQcCnt/monthlyMobileQcCnt/compIdx(낮음·중간·높음)`. 저빈도는 `"< 10"` 문자열.
- [x] 자동완성/연관 응답 포맷:
  - 네이버 `ac.search.naver.com/nx/ac?...&r_format=json` → `{ items: [[[추천어,…],…]] }` (각 항목 `[0]`)
  - 구글/유튜브 `suggestqueries.google.com/complete/search?client=firefox&ds=yt?` → `[query, [추천어…]]`
- [x] 블로그 제목 — `blog.naver.com/PostTitleListAsync.naver?blogId=&countPerPage=N` → JSON `{resultCode,postList:[{title(URL인코딩)}]}`. (JSON.parse 실패 대비 정규식 `"title":"([^"]*)"` 폴백 + URL 디코드)

> M1 스파이크와 동일 방식: `tools/spike/` 에 프로브 추가 → 실측값으로 셀렉터·기법 교체 후 착수.

---

## 구현 현황 & 설계 이탈 (현행화 2026-06-06)

### ② 주제 선정기 — 완료
| WP | 상태 | 비고 |
|---|---|---|
| WP1 경로 A | ✅ 완료 | 검색량·경쟁도(1-2) HMAC 서명 동작, 시계 배너(1-3). **이탈**: 1-5 xlsx 입출력 UI 제거 → **결과 테이블 다중 정렬로 대체**, xlsx 입출력은 **M4(대량/예약 발행)로 연기** (`src/lib/sheet.ts`·테스트는 보존) |
| WP2 경로 B | ✅ 완료 | PostTitleListAsync, blogId 현재탭 자동인식, 제목 클릭 → `Topic.title` |
| WP3 경로 C | ✅ 완료(이탈) | 다중 소스(네이버·구글·유튜브) on/off + 병합. **이탈**: 3-2 "두 결과 표/제목 추천" → **단일 목록 + 플랫폼 배지 + 정렬**로 통합. 3-3 "일괄 내보내기(SheetJS)" → **제거**(M4 연기) |
| WP4 공통 | ◑ 부분 | 4-2 `Topic` 수렴 ✅. 4-1 중단/진행률: 어댑터에 `AbortSignal` 배선됨, **UI 취소 버튼 미구현** |

**UI 개편(설계 외 결정)**: 09 S1 의 경로 3탭 대신 **탭 2개**(키워드=A 검색량+C 연관 한 화면 / 블로그 제목=B). 결과는 독립 `ResultTable`(검색 전 숨김, 다중 정렬). 경로별 결과 보관(탭 이동 시 유지). `Topic.source`/`sources` 필드 추가.

**구현 파일**: `src/adapters/topic/{searchad,blogtitles,autocomplete}.ts`, `src/lib/naver-sign.ts`, `src/components/topic/index.ts`, `entrypoints/background.ts`(topic.collect), `entrypoints/options/App.tsx`(검색광고 키 3필드), `entrypoints/sidepanel/App.tsx`. host_permissions: `api.searchad.naver.com`·`ac.search.naver.com`·`suggestqueries.google.com`.

### ④ 부가요소 합성기 / ⑥ 삽입 — 부분 (이전 마일스톤서 일부)
| WP | 상태 | 비고 |
|---|---|---|
| WP0-2 정합성 | ✅ | `composer/validate.ts` — AD 옵션 ON시 누락 차단(R-3.4) |
| WP0-3 표 컴포넌트 | ✗ 미사용 | TanStack Table 대신 자체 `ResultTable` |
| WP5 합성 | ◑ 부분 | 5-1 큐 생성·5-4 CTA/백링크/출처·5-5 AD 누락금지·5-6 빈마커 스킵 ✅. **5-2 위치 지정·5-3 상품표 autoUpdate 미구현** |
| WP6 삽입 | ◑ 부분 | 표·링크·광고문구 HTML paste 로 입력됨(스파이크 확인). 6-2 자동생성 텍스트 정리 미정교 |

### 기타 (M2 외 발생)
- `fix(insert)`: `handleInsert` 예외 시 무한 대기 → try/catch 안전 종료(R-4.4)
- `chore(dev)`: wxt dev 파일감시 멈춤 → `cross-env` 폴링 강제

### M2 잔여 (미완)
- WP4-1 수집 **중단/진행률 UI**
- WP5-2 부가요소 **위치 지정**, WP5-3 **상품표 autoUpdate**
- WP6 표/링크 삽입 **정식화**(현재 paste 의존), 자동생성 텍스트 정리
- 1-5/3-3 **xlsx 입출력** (M4 로 이관)
